#include <stdio.h>

#include "scene_manager.h"
struct Vertex* get_vertices_from_game_object(struct GameObject go) {
	// Apply Transform Matrix (Position, Rotation, Scale) to Mesh
	// to convert from Object space to World space
	// Then return the vertices	
}


